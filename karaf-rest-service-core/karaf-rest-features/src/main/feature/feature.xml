<?xml version="1.0" encoding="UTF-8"?>

<features name="karaf-rest-example-${project.version}" xmlns="http://karaf.apache.org/xmlns/features/v1.4.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://karaf.apache.org/xmlns/features/v1.4.0">

    <repository>mvn:org.apache.cxf.karaf/apache-cxf/${cxf.version}/xml/features</repository>

    <feature name="karaf-rest-datasource" version="${project.version}">
        <config name="org.ops4j.datasource-weather">
            osgi.jdbc.driver.class=org.mariadb.jdbc.Driver
            dataSourceName=weather
            databaseName=weather
            user=root
            password=root
            port=3306
        </config>
        <capability>
            osgi.service;objectClass=javax.sql.DataSource;effective:=active;osgi.jndi.service.name=weather
        </capability>
    </feature>

    <feature name="karaf-rest-common" version="${project.version}">
        <feature>transaction</feature>
        <feature>jndi</feature>
        <feature>pax-jdbc-config</feature>
        <feature>pax-jdbc-mariadb</feature>
        <feature>pax-jdbc-pool-dbcp2</feature>
        <feature>jdbc</feature>
        <feature dependency="true">aries-blueprint</feature>
        <feature version="[2,3)">jpa</feature>
        <feature version="[2,3)">eclipselink</feature>
        <feature version="[5,6)">hibernate</feature>
        <feature version="[3,4)">openjpa</feature>

        <bundle>mvn:com.weather.fetcher/karaf-rest-api/${project.version}</bundle>
    </feature>

    <feature name="karaf-rest-blueprint" version="${project.version}">
        <feature version="${project.version}">karaf-rest-common</feature>
<!--        <feature version="${project.version}">karaf-rest-persistence</feature>-->

        <requirement>osgi.service;effective:=active;filter:=(objectClass=org.osgi.service.http.HttpService)</requirement>
        <feature dependency="true">aries-blueprint</feature>
        <feature version="${cxf.version}" dependency="true">cxf-jaxrs</feature>

        <bundle dependency="true">mvn:com.fasterxml.jackson.core/jackson-core/${jackson.version}</bundle>
        <bundle dependency="true">mvn:com.fasterxml.jackson.core/jackson-annotations/${jackson.version}</bundle>
        <bundle dependency="true">mvn:com.fasterxml.jackson.core/jackson-databind/${jackson.version}</bundle>
        <bundle dependency="true">mvn:com.fasterxml.jackson.jaxrs/jackson-jaxrs-base/${jackson.version}</bundle>
        <bundle dependency="true">mvn:com.fasterxml.jackson.jaxrs/jackson-jaxrs-json-provider/${jackson.version}</bundle>
        <bundle>mvn:com.weather.fetcher/karaf-rest-blueprint/${project.version}</bundle>
    </feature>

    <feature name="karaf-rest-persistence" version="${project.version}">
        <feature version="${project.version}">karaf-rest-common</feature>
        <bundle>mvn:com.weather.fetcher/karaf-rest-persistence/${project.version}</bundle>
    </feature>

</features>
